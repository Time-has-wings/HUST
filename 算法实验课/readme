# 华中科技大学计算机科学与技术学院 算法实验课POJ题目题解
# Created by LINwhy on 2022/11/16
# Time 23:49


# 至2022/11/28共写了26题
# 对这26题进行算法分类和注意事项以及思路进行说明

# P1000 打印A+B 难度0
# P1005 处理浮点数，套公式即可
# P1753 枚举所有情况，最多2的16次方种情况。 注意，变化最多16次
# P3295 递归
# P2366 使用set即可
# P2503 使用map即可(相当注意本题的输入与输出)
# P3714 模仿最近点对的处理方法即可
# P3233 模仿快速幂取模算法，定义一个类来求解
# P2506 简单的一个高精度
# P1050 一端枚举所有可能，一端使用最大连续子数组的DP求解。(注意使用前缀和)
# P1088 DP前进行排序 难度不大
# P1185 状态压缩DP，洛谷上提高+/省选题。建议写另外几道题学习一下状态压缩DP，如洛谷P1896互不侵犯，P1879 [USACO06NOV]Corn Fields G】
# P1636 并查集求解连通分量，再枚举(或称之为背包问题)
# P2228 见《算法竞赛之进阶指南》中，“环形与后效性处理”一节
# P1042 可见北大MOOC，枚举最后到达哪个湖，再对每个湖泊可收获时间片可收获的鱼数进行排序
# P1328 简单的区间覆盖问题，对右端点进行排序
# P3040 贪心+小型模拟 贪心规则容易想到，重点在模拟过程
# P1700 对数据范围进行分类，n=1，n=2，n=3，n>=4这四种情况，最后一种情况有两种考虑方式(相关证明不是相当确定)
# P1860 spfa判正环(本题相当恶心，在POJ上一直过不了，但是在百练上可以过。)(网上找到的一篇两次floyd算法判负环可以过本题)
# P2387 简单的spfa算法
# P1062 枚举等级范围，用dijkstra跑一遍单源最短路。(注意可以引入超级源点解题)
# P3660 求解出传递闭包，在将传递闭包转置，二者相加后，所得矩阵中一列只有一个0的即为可以确定排名的cow
# P1201 差分约束，问题转换相当重要。详见《算法竞赛之进阶指南》之"负环与差分约束"
# P1324 状态压缩+bfs 状态压缩时，注意第一点，压缩信息尽可能小，第二点，状态转移的效率更可能高。同时，注意，本题卡map，查询状态是否已经出现时，需要O(1)查询
# P2449 A*算法 以每个点到终点的最短距离为估价函数值
# P1077 A*算法 + 状态压缩 以每个数字到预期位置的曼哈顿距离为估价函数 
